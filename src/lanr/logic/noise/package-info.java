/**
 * <p>Contains the implementations of the different noise recognition algorithms used by LANR.</p>
 *
 * <p>1. The abstract class {@link lanr.logic.noise.NoiseSearch} is used 
 * by every noise search algorithm.  
 * <br>
 * <p>2. The abstract class {@link lanr.logic.noise.FrequencySearch} extends {@link lanr.logic.noise.NoiseSearch} for more
 * functionality.
 * <br>
 * <p>3. The class {@link lanr.logic.noise.Filter} is not currently used.
 * <br>
 */
package lanr.logic.noise;
