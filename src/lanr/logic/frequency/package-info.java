/**
 * <p>Contains the frequency conversion methods used by LANR.</p>
 *
 * <p>1. The interface {@link lanr.logic.frequency.SampleConverter} is used by {@link lanr.logic.frequency.DiscreteCosineTransformer},
 * {@link lanr.logic.frequency.FastFourierTransformer} and {@link lanr.logic.frequency.FastWaveletTransformer}.  
 * <br>
 * <p>2. The enum {@link lanr.logic.frequency.FrequencyConversion} contains the different types of conversion.
 * <br>
 */
package lanr.logic.frequency;
